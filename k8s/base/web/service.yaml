apiVersion: v1
kind: Service
metadata:
  name: web
  namespace: horsetraining
  labels: { app: horsetraining, service: web }
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: http
      name: http
  selector: { app: horsetraining, service: web }
